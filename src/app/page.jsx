'use client';
import ScrollableFeed from 'react-scrollable-feed';

export default function Home() {
  return (
    <ScrollableFeed className="bg-gradient-to-bl from-lime from-[1%] via-aqua to-[96%] via-[70%] to-orange h-full w-full flex flex-col items-center relative justify-between shadow-2xl overflow-hidden">
    </ScrollableFeed>
  );
}